version: '3.9'
services:
    nextjs-app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3001:3000'
        environment:
            NODE_ENV: production
        volumes:
            - .:/app
            - /app/node_modules
        command: ['npm', 'run', 'start']
        restart: unless-stopped
